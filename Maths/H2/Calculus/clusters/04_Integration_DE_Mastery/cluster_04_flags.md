# Flags — Calculus > Integration_DE_Mastery (Cluster 4)

> Append-only. Gaps surfaced from the 2026-06-24 pre-study session on Blocks 26–46.

## 2026-06-24 — gaps flagged from pre-study walkthrough

- ACTIVE — Trig power integration reflex (sec²x, sin²x, cos²x, tan²x). Tried to convert sec²x to cos⁻²x and power-rule it. Trig powers ALWAYS need identity rewrite first; sec²x is the direct exception (it's the derivative of tan x).
- ACTIVE — `a` vs `a²` in MF27 standard forms. Confused the constant with `a` (e.g. saw `1/(4+9x²)` and treated 4 as `a` instead of `a²`).
- ACTIVE — Substitution chain incomplete: forgot trig identity is needed when integrand reduces to cos²θ mid-substitution.
- ACTIVE — IBP formula recall. Forgot `∫u dv = uv − ∫v du` twice. Also did not simplify the `∫v du` integrand algebraically before integrating.
- ACTIVE — DE pattern recognition. Three patterns (∝N, ∝(K−N), ∝N(K−N)) not snap-recognised; had to reason through each.
- ACTIVE — DE substitution mechanics. Did not understand WHY you differentiate the given substitution at the start. Struggled with chain through y = ux and v = x+y.
- ACTIVE — DE interpretation vocabulary. "Carrying capacity", "long-term behaviour", "equilibrium" not defined precisely. Explicit request: define these clearly in study sheets.
- FRAGILE — Partial fractions trigger. Asked "why can't I just expand?" Trigger is: rational function with factorable denominator (multiple distinct factors). Single denominator → use Block 26/28 instead.
- FRAGILE — Repeated IBP. Did not know to apply IBP twice for (ln y)² in volume-of-revolution; eventually got there.
- FRAGILE — Algebra hygiene: `e^(a+b) = e^a · e^b` split forgotten momentarily.
- FRAGILE — Inverse-trig integration recognition (forgot for tan⁻¹ form mid-DE).

## Format reminder
`YYYY-MM-DD | card # | what went wrong | confidence | gap state`
