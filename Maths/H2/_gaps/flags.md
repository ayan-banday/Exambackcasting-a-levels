# Maths Gap Flags — append-only

## 2026-07-26 · Maths Test 1 (Calculus + Vectors) · self-score ~60%

Confidence vs correctness (from Loom transcript):

### PRIORITY 1 — confident and WRONG
- **Q1(b) reverse-chain constant.** Integrating x*sqrt(4-x^2): put -2 out front instead of -1/2. Root cause: confused "balance the x" with the substitution constant. For int x*f(x^2): u=inner, du=(deriv)dx, so x dx = (1/deriv) du — the constant is a DIVISION, not a multiplication. He was "very confident", got it wrong.

### ACTIVE — did not have the method / rule
- **Q2 cyclic-IBP vs double-IBP.** Took very long; had no rule. Resolved himself: cyclic only for e^(ax)*(sin/cos) that regenerates; if the factor decays (ln^n, x^n) it's plain repeated IBP. Wants this as a firm rule.
- **Q3 partial-fraction DE chain.** (a) partial-fraction setup sign error (thought B was a whole number — "if B is a whole number you're wrong"). (b) combining the two logs with the 1/10 constant out front. (c) removing the constant before exponentiating (x times 10 first). (d) the +/- e^c = A step. (e) reading "in thousands" -> initial conditions. (f) interpreting the LIMITING value (x -> carrying capacity 10, not the exponential -> infinity).
- **Q4 Maclaurin.** Differentiation/simplification error in the 2nd derivative (didn't spot -sin x - sin^2 - cos^2 = -(1+sin x) to cancel). Also unsure what a Maclaurin series IS and that dy/dx links to it; and using the series to evaluate the limit ("hence").
- **Q6(c)(d) / vectors formulas FORGOTTEN.** Scalar projection (a.b/|a|), vector projection, area of triangle (1/2|a x b|), foot of perpendicular. Knew once, blanked.
- **Q7(b) related rates.** Tried to introduce a new variable l (slant) instead of expressing the slant surface area in the single variable h. Also unsure of curved surface area = pi*r*l.

### FRAGILE / careless (method known, slipped)
- **Q2(b)** careless substitution when evaluating at x=e. Fix he set: write out the substitution explicitly.
- **Q7(a), Q6(a)(b), Q1(a)(c)** all correct and confident.

### Process feedback from Ethan
- Model answers should be segmented ONE LINE PER MARK (like the school answer key), not crammed.
- Vary DE/question contexts — do NOT reuse "spreading rumour"; he pattern-matches on context.
- Paper difficulty decent but each question slightly long; the real paper is ~30% harder — push there next time.
- Q5 was mis-scoped (skew lines = Lines & Planes, not yet learnt) — already swapped to a vectors-basics question.

### Timing (Math Test 1)
- 7 questions took ~1h 50min. Calibration: a 2h Maths test = **8 questions covering everything** (build a full paper's worth). Log actual time each test. Chem timing TBD (Ethan will report).
